# DataSentinel 迭代优化建议 (Iteration Suggestions)

在对 DataSentinel 项目的当前架构和功能实现进行全面分析后，为您提供以下五个维度的迭代优化建议，以进一步提升工具的实用性、性能和用户体验。

## 1. 文件格式支持扩展 (File Format Support)
当前系统已经支持了主流的纯文本和新版 Office 文档，但为了覆盖更多的企业级场景，建议：
* **PDF 文档支持**: 许多合同、发票和机密文档都以 PDF 格式存储。可以引入类似于 `pdfcpu` 或 `ledongthuc/pdf` 的纯 Go 库来提取 PDF 文本并进行扫描。
* **压缩包递归扫描**: 增加对 `.zip`、`.tar.gz`、`.rar` 等常见归档格式的支持，并在内存中解压或流式读取内部文件进行递归扫描。
* **老版本 Office 及富文本**: 增加对旧版 Office 格式（`.doc`, `.xls`）及 RTF 等格式的文本提取支持。
* **OCR 图像文本提取 (长远规划)**: 很多凭证（如身份证正反面、银行卡照片）是图片格式。可以考虑集成轻量级的本地 OCR 引擎（如 Tesseract 或 Go 包装器）来扫描 `.png`、`.jpg` 图片中的敏感信息。

## 2. 检测能力升级 (Detection Capabilities)
当前的 10 种内置规则已经涵盖了常见的敏感数据类型，但仍有较大的扩展空间：
* **自定义检测规则**: 允许用户通过 UI 或加载本地 JSON/YAML 配置文件，添加自定义的正则表达式和验证逻辑，满足不同企业的特定脱敏需求。
* **更多云厂商与令牌支持**: 扩展云厂商及流行 SaaS 服务的 Token 规则，例如 Azure Keys, GCP Service Account JSON, Slack Tokens, Stripe Keys 等。
* **降低误报率 (上下文分析增强)**: 简单的正则匹配容易产生误报。可以结合前缀、后缀关键词（如 `password:`, `api_key=`）或者基于简单的 NLP 算法和信息熵进行二次过滤。
* **自动脱敏/打码功能 (Data Masking)**: 针对纯文本或特定格式文件，提供“一键脱敏”功能，生成一份替换了敏感信息（用 `*` 号覆盖）的副本文件。

## 3. 性能与架构优化 (Performance & Architecture)
系统目前采用了多核并发扫描管道，整体架构轻量高效，但处理超大目录或大文件时可进一步优化：
* **流式读取与大文件处理**: 当前处理机制对于超过 50MB 的文件会直接跳过或需要加载入内存。可采用 `bufio` 流式读取或 `mmap` (内存映射)，有效降低内存峰值，甚至支持上 GB 级别日志文件的扫描。
* **增量扫描机制**: 在本地使用 SQLite 或特定文件记录文件的 Hash 值与最后修改时间。在下一次扫描同一目录时，仅扫描有变动或新增的文件，大幅提升日常例行扫描速度。
* **动态资源调度**: 增加配置选项，允许用户限制扫描时的 CPU 核心数和最大内存占用，避免在办公环境下运行造成系统卡顿。

## 4. CLI 模式与集成能力 (CLI & Integration)
作为一个单文件执行工具，扩展其使用场景非常有价值：
* **无头 CLI 模式 (Headless Mode)**: 提供命令行参数（如 `datasentinel.exe --scan "C:\code" --output report.json --headless`），运行时不打开浏览器，直接在终端输出进度并生成报告。
* **CI/CD 管道集成**: 支持导出 SARIF 或 JUnit XML 格式的报告文件，方便集成到 Jenkins、GitLab CI 或 GitHub Actions 中作为安全扫描节点。
* **Git Pre-commit Hook 集成**: 提供生成 Git 钩子的命令，允许开发者在每次 `git commit` 前自动扫描改动的文件，从源头防止敏感信息泄露。

## 5. UI/UX 体验增强 (UI/UX Enhancements)
目前的内置 Web UI 非常直观，但还可以提供更多高级特性：
* **扫描历史记录管理**: 将历次扫描的结果保存在本地轻量级数据库（如 SQLite 或 JSON 文件）中，用户打开软件后可以浏览过去的扫描报告，无需每次重新扫描。
* **高级可视化与导出**: 在统计图表页增加更多维度的分析（如按目录分布、按文件类型分布）。支持导出更详细的 Excel 报告（带多工作表分类）。
* **国际化 (I18N) 支持**: 虽然当前同时有中英说明，但在界面中增加明显的语言切换按钮（中文/英文），有利于推广给全球用户。
* **文件上下文高亮预览**: 在查看匹配详情时，利用类似 Monaco Editor 或简单的富文本组件，将前后几十行的代码上下文渲染出来，并将敏感词高亮，方便用户更快速地判定是否为误报。
